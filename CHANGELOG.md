# 发布记录

## release0.1

首次 GitHub 发布，保存当前程序 0.3.2 的功能快照。`release0.1` 是发布标签，Python 包版本保持 0.3.2，避免更改现有安装和缓存版本关系。

- Docling 结构提取，结合原生字形与图内 OCR，程序完成 PDF 双语排版。
- OpenAI 兼容接口；JSON、带 ID 的文本及单目标纯文本协议。
- 全文语境与术语准备、动态组页、一次模型复核；仅 OCR 页面附图。
- 单主模型异步并发、按 RPM / TPM 预算调度、有限重试及备用模型。
- 保留部分成功译文；局部故障不丢弃其它页面，重试耗尽仍尽力导出。
- 191 项离线回归测试，以及匿名化的完整端到端运行摘要。

已知限制：当前支持 PDF；复杂公式与文字混排、浅色标签、图形标记附近仍可能出现漏译或碰撞，自动 QA 不能代表高保真验收通过。

发布范围仅含源码、合成测试、示例配置、审核过的辅助脚本及脱敏文档。密钥、个人配置、课件和运行产物留在本地。
