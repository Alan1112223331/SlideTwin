# Prefer an environment variable. Never commit a real key.
SLIDETWIN_API_KEY=

# HTTP API access token, separate from the translation provider key.
# Generate: python -c "import secrets; print(secrets.token_urlsafe(32))"
SLIDETWIN_API_TOKEN=
