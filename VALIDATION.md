# 验证记录

## 0.4.0 Docker / API 验证

2026-09-24：207 项本地离线测试通过，包含 16 项新增服务测试；Linux CPU Docker 镜像实际构建并运行，容器依赖检查通过。

两页合成课件通过 HTTP 上传，使用配置的 Qwen 主模型完成术语、初译和复核，得到 2 页中文 PDF、4 页中英对照 PDF、中文 / 中英 JSON 与 Markdown，以及原版 Docling JSON / Markdown，共 8 个内容文件和 1 个报告。11 个译文块完整，中文页与中英文件中的译文页逐像素一致，原文页保持不变；全部译文可从 PDF 中提取，最终两张译文页经 Poppler 渲染查看。

容器调试发现并修复了 RapidOCR 非 root 缓存写入权限、中文字体的兼容字形映射问题。字体修复后复用已有译文重新排版，未追加模型调用；该记录不宣称最初构建的镜像一次无错误。3 次模型请求中术语请求达到输出上限并保留已返回内容，初译和复核正常结束。

另一个未配置模型密钥的容器完成了 Docling-only 提取，实测约 14 秒（模型缓存已就绪），没有任何模型请求。实际重启运行中的任务后，任务从持久化状态恢复并完成。鉴权、大小 / 队列限制、局部结果留存、下载白名单及异步补位另有离线测试。

合成样品和运行凭据留在本地，不随仓库上传。这些验证覆盖当前容器与接口路径，不代表任意复杂课件已通过高保真视觉验收。

## release0.1 历史快照

GitHub 发布：`release0.1`。程序版本：0.3.2。

## 离线测试

191 项测试通过，依赖检查通过。测试通过合成 PDF、虚构姓名、虚构课程编号和模拟 HTTP 响应运行，不需要真实课件或 API Key。

覆盖内容包括上下文预算、同模型异步并发、OCR 附图来源、普通文本协议、失败重试、备用模型、部分结果留存、局部排版故障隔离及输出保护。

发布前使用仅含待发布文件的干净副本重新执行测试，确保测试不依赖本地私有脚本或课件。

## 最近一次真实端到端运行（匿名摘要）

一次从空工作目录开始的完整执行，将 19 页输入生成了 38 页双语 PDF，总耗时 424.09 秒，包含提取、翻译、复核、排版和预览。全部 19 页合为一个翻译组，125 个目标；术语、初译、复核共 3 次模型调用，全部首次成功，没有使用缓存译文、重试或备用模型。

此次真实运行未遇到调用失败，因此不以此证明重试分支生效；重试行为由离线故障注入测试验证。

原始文件未修改，译文页在定义的编辑区域外没有像素变化。两张原文页的最外边缘存在最大 1 的颜色通道舍入差，内容区域未变化。

## 已知质量限制

看图发现封面孤字换行、浅色标签漏译、保留公式中夹杂英文，以及译文与公式或圆形标记、箭头碰撞。因此该样品虽然完整输出且通过自动检查，仍未通过高保真视觉验收。

程序当前的 `automated_checks_passed` 不能替代视觉检查，也不能证明专业语义完全正确。模型复核与程序排版检查分别负责不同问题。

## 数据边界

源课件、输出样品、逐页图片、原始请求响应、账户额度、本机路径和含私人信息的运行报告不随仓库或 Release 发布。本文件仅保留匿名化指标，不提供私有样品下载。
